import "./globals.css";

export const metadata = {
  title: "Generator LN — Prediksi LN TOP & Patah",
  description:
    "Generator LN deterministic: masukkan result 4D, dapatkan LN TOP, Patah 1-4, dan Kress Ai secara instan.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  );
}
